import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'
import dotenv from 'dotenv'
// dotenv.config({path: 'D:/proj_vuejs/weather/.env'});

createApp(App).mount('#app')
